---
const { current = 1, total = 1, base = '/mauricionlz2018.github.io/Blog/page' } = Astro.props;
---

<nav class="flex items-center justify-center gap-3 my-6">
  {current > 1 ? (
    <a class="px-3 py-1 rounded-md bg-gray-200 hover:bg-gray-300" href={`${base}/${current - 1}`} rel="prev">Anterior</a>
  ) : (
    <span class="px-3 py-1 text-gray-400">Anterior</span>
  )}

  <span class="px-3 py-1">{current} / {total}</span>

  {current < total ? (
    <a class="px-3 py-1 rounded-md bg-gray-200 hover:bg-gray-300" href={`${base}/${current + 1}`} rel="next">Siguiente</a>
  ) : (
    <span class="px-3 py-1 text-gray-400">Siguiente</span>
  )}
</nav>
